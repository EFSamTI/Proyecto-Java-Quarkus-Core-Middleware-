services:
  entities:
    build:
      context: .
      dockerfile: entities.Dockerfile
  environment:
    - PG_URL=${PG_URL}
    - PG_USER=${PG_USER}
    - PG_PWD=${PG_PWD}
    - MONGO_URL=${MONGO_URL}
    - MONGO_DB=${MONGO_DB}
  ports:
    - "8091:8091"
